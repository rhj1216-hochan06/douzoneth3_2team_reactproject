import { Button } from "react-bootstrap";
import { manRecommend } from "../components/recommend/manRecommend.js";

const manRecommend = ({convertPrice}) => {

      return <manRecommend 
      convertPrice={convertPrice}
      />;
    
};

export default manRecommend;
